# AES-CTR Encryption Example

This repository contains a simple Python script demonstrating file encryption 
using the Advanced Encryption Standard (AES) in Counter Mode (CTR).

## Requirements

To run this code, you must have the following library installed:
```bash
pip install pycryptodome
